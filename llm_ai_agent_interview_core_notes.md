# LLM / AI Agent / 前后端面试核心知识点速记版

> 目标：只保留面试中最核心、最常被追问的知识点。每个点用一句话解释。

## 1. LLM 基础

- **LLM**：大语言模型，本质是基于上下文预测下一个 token 的概率模型。
- **Token**：模型处理文本的基本单位，影响上下文长度、成本和延迟。
- **上下文窗口**：模型一次请求能看到的最大 token 数，包含 system prompt、历史消息、检索内容和输出。
- **Temperature**：控制输出随机性，越低越稳定，越高越发散。
- **Top-p**：只从累计概率达到 p 的候选 token 中采样，用于控制生成多样性。
- **幻觉**：模型生成看似合理但不真实的内容，常由上下文不足、检索差或约束弱导致。

## 2. 机器学习基础

- **机器学习**：让模型从数据中学习规律，并对新样本做预测或决策。
- **监督学习**：用有标签数据学习输入到标签的映射，常见任务是分类和回归。
- **无监督学习**：从无标签数据中发现结构，如聚类、降维、异常检测。
- **强化学习**：智能体通过和环境交互，根据 reward 学习策略。
- **分类 vs 回归**：分类输出离散类别，回归输出连续数值。
- **训练集 / 验证集 / 测试集**：训练集学参数，验证集调参，测试集做最终评估。
- **过拟合**：训练集好、测试集差，说明模型记住噪声，泛化差。
- **欠拟合**：训练集和测试集都差，说明模型能力不足或特征不够。
- **Bias / Variance**：高偏差易欠拟合，高方差易过拟合。
- **损失函数**：衡量预测和真实值差距，是模型优化目标。
- **梯度下降**：沿负梯度方向更新参数，使 loss 下降。
- **学习率**：控制每次参数更新步长，太大不稳定，太小收敛慢。
- **正则化**：限制模型复杂度，降低过拟合。
- **Dropout**：训练时随机丢弃部分神经元，减少过拟合。
- **数据泄漏**：训练中使用预测时拿不到的信息，会导致离线效果虚高。
- **类别不平衡**：少数类样本很少时 accuracy 可能失真，应关注 F1、AUC、PR-AUC。
- **Precision**：预测为正的样本中有多少是真的正。
- **Recall**：真实为正的样本中有多少被找出来。
- **F1**：Precision 和 Recall 的调和平均。
- **ROC-AUC / PR-AUC**：衡量分类排序能力，类别极不平衡时 PR-AUC 更敏感。
- **MSE / MAE / RMSE / R²**：常见回归指标，分别衡量误差大小和解释能力。

## 3. 常见传统 ML 模型

- **逻辑回归**：常用于二分类，用 sigmoid 输出正类概率。
- **决策树**：用一系列 if-else 规则划分样本，可解释但易过拟合。
- **随机森林**：多棵决策树投票或平均，降低单棵树方差。
- **GBDT**：Boosting 思想，后一棵树修正前面模型错误。
- **XGBoost / LightGBM**：高效 GBDT 工程实现，表格数据常用。
- **SVM**：寻找最大间隔超平面，核函数可处理非线性问题。
- **KNN**：根据最近的 k 个样本投票或平均，训练简单但推理慢。
- **朴素贝叶斯**：基于条件独立假设，常用于文本分类。
- **K-Means**：无监督聚类算法，需要预先指定 k。
- **PCA**：线性降维方法，保留数据最大方差方向。
- **交叉验证**：多次划分训练和验证集，得到更稳定的评估结果。
- **超参数调优**：调整学习率、树深、batch size 等训练前设定的参数。

## 4. 传统 NLP

- **分词**：把文本切成词或子词，中文 NLP 常需要处理词边界。
- **One-hot**：用稀疏向量表示词，无法表达语义相似性。
- **TF-IDF**：结合词频和词稀有度，衡量词对文档的重要性。
- **N-gram**：连续 n 个词或字符片段，用于引入局部词序信息。
- **传统语言模型**：用统计方式估计词序列概率，常见是 n-gram。
- **平滑**：给未见过的 n-gram 分配非零概率，缓解数据稀疏。
- **Word2Vec**：通过 CBOW 或 Skip-gram 学习静态词向量。
- **GloVe**：利用全局词共现统计学习词向量。
- **静态词向量问题**：一个词只有一个向量，无法处理一词多义。
- **RNN**：按序列递归处理文本，难并行且长距离依赖弱。
- **LSTM / GRU**：用门控机制缓解 RNN 长距离依赖问题。
- **Seq2Seq**：Encoder 编码输入，Decoder 生成输出，常用于翻译和摘要。
- **Attention**：生成时动态关注输入不同位置，缓解固定向量瓶颈。

## 5. Transformer

- **Self-Attention**：让每个 token 根据相关性聚合其他 token 信息。
- **Q / K / V**：Q 查询信息，K 用于匹配，V 提供实际内容。
- **Attention 公式**：`softmax(QK^T / sqrt(d_k))V`。
- **除以 sqrt(d_k)**：防止点积过大导致 softmax 饱和。
- **Mask**：控制 token 可见范围，GPT 用 causal mask 防止看未来。
- **Multi-Head Attention**：多个 head 从不同子空间学习不同关注模式。
- **FFN**：对每个 token 独立做非线性变换，增强表达能力。
- **残差连接**：缓解梯度消失，使深层网络更稳定。
- **LayerNorm**：稳定激活分布，适合 NLP 变长序列。
- **位置编码**：让 attention 感知 token 顺序。
- **RoPE**：通过旋转方式把位置信息注入 Q/K，现代 LLM 常用。
- **Encoder-only**：如 BERT，双向理解，适合分类、匹配、NER。
- **Decoder-only**：如 GPT，自回归生成，是现代 LLM 主流架构。
- **Encoder-Decoder**：如 T5，适合输入到输出转换任务。
- **复杂度**：标准 self-attention 对序列长度是 O(n²)。
- **FlashAttention**：不改公式，优化 GPU 显存读写和 attention 计算速度。
- **KV Cache**：推理时缓存历史 K/V，加速逐 token 生成，但占显存。

## 6. 大模型训练

- **生命周期**：数据收集、清洗、tokenization、预训练、SFT、对齐、评估、部署、监控。
- **预训练**：用海量无标注文本做 next token prediction，学习通用语言能力。
- **Tokenizer**：把文本转成 token id，影响成本、上下文利用率和模型效果。
- **语言模型 Loss**：通常是交叉熵，衡量下一个 token 预测是否正确。
- **Batch Size**：每次更新使用的样本量，大 batch 更稳定但更耗显存。
- **混合精度**：FP16/BF16 降低显存和提升吞吐，BF16 稳定性更好。
- **分布式训练**：用数据并行、张量并行、流水线并行、ZeRO 解决单卡限制。
- **训练显存来源**：模型参数、梯度、优化器状态和激活值。

## 7. 微调与对齐

- **SFT**：用高质量指令-回答数据继续训练模型，让模型更会按指令回答。
- **Alignment**：让模型输出更符合人类偏好、安全规范和业务要求。
- **RLHF**：用人类偏好训练 reward model，再通过强化学习优化模型。
- **DPO**：直接用 chosen/rejected 偏好对训练，流程比 RLHF 简化。
- **Fine-tuning vs RAG**：缺外部知识优先 RAG；格式、风格、任务习惯不稳定可微调。
- **全量微调**：更新所有参数，效果强但成本高、易遗忘。
- **PEFT**：冻结大部分参数，只训练少量新增参数。
- **LoRA**：冻结原模型，训练低秩增量矩阵，常用于低成本适配。
- **QLoRA**：量化加载基座模型，只训练 LoRA，显著降低微调显存。
- **Chat Template**：微调格式必须和模型推理对话格式一致。
- **数据配比**：业务数据和通用数据混合可降低过拟合和能力退化。
- **Gradient Accumulation**：多次累积梯度再更新，模拟更大 batch。
- **微调评估**：不能只看 loss，还要看任务准确率、格式遵守、幻觉、安全和通用能力。

## 8. 推理与部署

- **Prefill**：处理完整输入并建立 KV cache，长输入会提高首 token 延迟。
- **Decode**：逐 token 生成输出，长输出会拉长总耗时。
- **量化**：用 INT8/INT4 等低精度降低显存和成本，但需评估质量损失。
- **蒸馏**：让小模型模仿大模型，用于降本和加速。
- **Speculative Decoding**：小模型先猜，大模型验证，以减少大模型 decode 步数。
- **模型路由**：简单任务走小模型，复杂任务走大模型，平衡质量、延迟和成本。

## 9. Embedding 与 RAG

- **Embedding**：把文本映射成向量，语义相近的文本距离更近。
- **相似度**：常用 cosine、dot product 或 euclidean distance。
- **RAG**：先检索外部知识，再把相关片段放入 prompt 让模型回答。
- **RAG 流程**：文档解析、切分、向量化、入库、检索、rerank、拼 prompt、生成、引用。
- **Chunk 切分**：太大引入噪声，太小丢语义，应结合标题、段落和 overlap。
- **Rerank**：对召回片段重新精排，提高最终上下文相关性。
- **Hybrid Search**：BM25/关键词检索 + 向量检索，兼顾精确匹配和语义匹配。
- **Metadata Filter**：按权限、时间、文档类型等过滤检索结果。
- **RAG 常见问题**：检索不到、检索到但答错、答案不完整、延迟高。
- **向量数据库**：存储向量和 metadata，支持 ANN 相似度检索和过滤。

## 10. Prompt Engineering

- **Prompt 结构**：角色、任务、上下文、约束、示例、用户问题。
- **Zero-shot**：不给示例直接完成任务，适合简单任务。
- **Few-shot**：给示例让模型模仿格式和标准，适合复杂格式和分类规则。
- **结构化输出**：用 JSON schema、函数调用、校验和重试保证可解析。
- **Prompt Injection**：用户输入恶意指令试图覆盖系统指令或泄露信息。
- **防注入**：隔离系统指令和用户输入，限制工具权限，校验输出和工具参数。

## 11. AI Agent

- **Agent**：能围绕目标规划、调用工具、观察结果并继续行动的系统。
- **核心组成**：LLM、Tools、Memory、Planner、Executor、Evaluator。
- **Tool Calling**：模型生成结构化工具参数，后端负责校验和执行。
- **ReAct**：Reasoning + Acting，交替推理、调用工具、观察结果、继续决策。
- **Memory**：短期记录当前任务状态，长期记录偏好或历史知识。
- **Agent 失败模式**：工具选错、参数错、循环、未验证结果、误解目标。
- **防护方式**：限制最大步数、schema 校验、工具超时、权限控制、关键步骤确认。
- **Multi-Agent**：多 agent 分工协作，适合复杂任务，但成本和不稳定性更高。

## 12. 复杂度

- **时间复杂度**：算法步骤数随输入规模增长的趋势。
- **空间复杂度**：额外内存随输入规模增长的趋势。
- **O(1)**：常数复杂度，如数组下标访问。
- **O(log n)**：每次问题规模减半，如二分查找。
- **O(n)**：线性遍历一次输入。
- **O(n log n)**：常见于排序和分治。
- **O(n²)**：常见于双重循环枚举 pair。
- **O(2^n)**：常见于子集枚举。
- **O(n!)**：常见于全排列。
- **递归空间**：主要看递归栈深度。
- **BFS / DFS**：树通常 O(n)，图邻接表通常 O(V + E)。
- **哈希表**：平均查找 O(1)，最坏可能退化。
- **堆**：插入和删除堆顶 O(log n)，访问堆顶 O(1)。
- **DP 复杂度**：状态数量 × 每个状态转移成本。
- **滑动窗口 O(n)**：左右指针都只单调移动。
- **单调栈 O(n)**：每个元素最多入栈一次、出栈一次。

## 13. TCP/IP 与 HTTP

- **TCP/IP 四层**：应用层、传输层、网络层、链路层。
- **TCP**：面向连接、可靠、有序，适合 HTTP、文件传输。
- **UDP**：无连接、不保证可靠，延迟低，适合音视频、游戏、QUIC。
- **三次握手**：SYN、SYN+ACK、ACK，用于建立连接。
- **四次挥手**：FIN、ACK、FIN、ACK，用于关闭连接。
- **TIME_WAIT**：确保最后 ACK 到达，并让旧报文自然消失。
- **HTTP**：应用层协议，由 method、URL、headers、body 组成。
- **GET / POST / PUT / PATCH / DELETE**：分别用于查询、提交、整体更新、局部更新、删除。
- **状态码**：2xx 成功，3xx 重定向，4xx 客户端问题，5xx 服务端问题。
- **HTTPS**：HTTP + TLS，提供加密、完整性和身份认证。
- **HTTP/2**：多路复用、二进制分帧、Header 压缩。
- **HTTP/3**：基于 QUIC/UDP，弱网表现更好。
- **Cookie / Session / Token**：分别偏客户端存储、服务端会话、API 鉴权。
- **CORS**：浏览器跨域安全策略，需要服务端配置允许来源。
- **SSE**：服务端单向推送，适合 LLM token 流式输出。
- **WebSocket**：双向实时通信，适合实时协作和控制台。
- **幂等性**：同一请求执行一次和多次结果一致，重试设计必须考虑。

## 14. 操作系统与并发

- **进程**：资源分配单位，有独立地址空间。
- **线程**：CPU 调度单位，同进程线程共享内存。
- **协程**：用户态轻量执行单元，适合大量 I/O 等待。
- **并发 vs 并行**：并发是交替推进，并行是真正同时执行。
- **同步 / 异步**：关注结果如何返回。
- **阻塞 / 非阻塞**：关注等待时线程是否挂起。
- **上下文切换**：保存和恢复任务状态，线程过多会降低吞吐。
- **临界区**：访问共享资源的代码区域。
- **竞态条件**：多个线程读写共享数据导致结果不确定。
- **锁**：保护共享资源，但会降低并发度。
- **死锁**：多个线程互相等待资源，常通过固定加锁顺序避免。
- **读写锁**：读多写少时允许多读单写。
- **原子操作**：适合简单计数器和状态标记。
- **线程池**：任务队列 + 固定工作线程，控制并发度。
- **任务队列**：异步处理耗时任务，如文档解析、embedding、邮件发送。
- **I/O 密集**：适合异步、协程、线程池。
- **CPU 密集**：适合多进程、多线程、GPU 或 C++ 优化。
- **Python GIL**：限制多线程执行 Python 字节码，CPU 密集任务通常用多进程。

## 15. Docker 与部署

- **Docker**：把应用和依赖打包成容器，保证环境一致。
- **Image**：镜像，静态运行模板。
- **Container**：容器，镜像运行后的实例。
- **Dockerfile**：定义镜像构建步骤。
- **Volume**：持久化数据，容器删除后数据不丢。
- **Network**：容器间通信网络。
- **端口映射**：`host_port:container_port`，把容器服务暴露到宿主机。
- **Docker Compose**：用一个配置文件管理多容器服务。
- **部署关注点**：配置、日志、健康检查、环境变量、数据持久化、回滚。

## 16. LLM 应用后端

- **Model Gateway**：统一封装不同模型，支持路由、fallback、限流、成本统计。
- **流式输出**：后端接收模型 token chunk，再通过 SSE/WebSocket 推给前端。
- **取消生成**：用户停止时应取消上游模型请求，释放资源和节省成本。
- **限流**：按用户、API key、tenant、IP 控制请求频率。
- **Timeout**：模型调用、工具调用和 SSE 都要设置超时。
- **Retry**：只对临时错误有限重试，非幂等操作不能盲目重试。
- **日志**：记录 request id、模型版本、prompt 版本、token、延迟、错误。
- **权限控制**：RAG 和工具调用必须校验用户是否有访问权限。
- **缓存**：可缓存 embedding、检索结果、高频回答，但要注意权限和版本。

## 17. 数据库 / SQL

- **SQL 索引**：提升查询速度，但会增加写入和存储成本。
- **B+ 树索引**：数据库常用索引结构，适合范围查询。
- **联合索引**：遵循最左前缀原则。
- **事务 ACID**：原子性、一致性、隔离性、持久性。
- **隔离级别**：解决脏读、不可重复读、幻读等问题。
- **JOIN**：用于多表关联查询。
- **EXPLAIN**：分析 SQL 执行计划，定位慢查询。
- **慢查询优化**：加索引、改 SQL、减少全表扫、拆分大查询、缓存热点数据。

## 18. Redis / 缓存

- **Redis**：内存型 key-value 数据库，常用于缓存、限流、队列和分布式锁。
- **常见数据结构**：String、Hash、List、Set、ZSet。
- **缓存穿透**：查询不存在数据，可用空值缓存或布隆过滤器。
- **缓存击穿**：热点 key 过期导致大量请求打到数据库，可用互斥锁或逻辑过期。
- **缓存雪崩**：大量 key 同时过期，可设置随机过期时间。
- **分布式锁**：用 Redis 控制多实例并发访问共享资源。
- **限流**：用计数器、滑动窗口或令牌桶限制请求频率。

## 19. 高频开放题答题框架

- **如何降低幻觉**：优化 RAG、要求引用、约束 prompt、拒答不确定问题、做评估。
- **RAG vs 微调**：知识问题用 RAG，行为/格式/风格问题用微调。
- **如何优化推理延迟**：缩短输入、限制输出、流式、量化、小模型、batching、缓存。
- **模型效果不好怎么办**：区分检索、prompt、模型能力、数据问题，再逐项优化。
- **如何设计企业知识库问答**：文档权限、解析切分、embedding、检索、rerank、引用、评估。
- **如何做灰度和回归**：固定评估集，小流量上线，监控质量、延迟、成本，支持回滚。
- **如何设计 LLM 推理服务**：API 鉴权限流、Model Gateway、推理框架、流式、监控、降级。
- **如何处理批量 LLM 限流**：队列、并发控制、指数退避、quota、失败恢复。
